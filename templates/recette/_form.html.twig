{{ form_start(form, {'attr': {'class': 'my-form'}}) }}
    {# Display global form errors #}
    {{ form_errors(form) }}

    <div class="form-group">
        {{ form_label(form.typeDePreparation, 'Nom:', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(form.typeDePreparation, {'attr': {'class': 'form-control'}}) }}
        {# Display field errors #}
        {{ form_errors(form.typeDePreparation) }}
    </div>
    
    <div class="form-group">
        {{ form_label(form.ingredient, 'Description:', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(form.ingredient, {'attr': {'class': 'form-control'}}) }}
        {# Display field errors #}
        {{ form_errors(form.ingredient) }}
    </div>

 

    
     <div class="form-group" style="margin-top:20px;">
        {{ form_label(form.Picture, 'Image:', {'label_attr': {'class': 'control-label'}}) }}
        {{ form_widget(form.Picture, {'attr': {'class': 'form-control-file'}}) }}
        {# Display field errors #}
        {{ form_errors(form.Picture) }}
    </div>

  

<div style="margin-top:20px;">
    <button type="submit" class="btn btn-primary">{{ button_label|default('Save') }}</button>
        <a href="{{ path('app_plante_index') }}" class="btn btn-outline-primary">back to list</a>
</div>
{{ form_end(form) }}
